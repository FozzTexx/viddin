#!/bin/sh
#
# Copyright 2015 by Chris Osborn <fozztexx@fozztexx.com>
#
# This file is part of viddin.
#
# This program is free software: you can redistribute it and/or modify
# it under the terms of the GNU General Public License as published by
# the Free Software Foundation, either version 2 of the License, or
# (at your option) any later version.
#
# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY; without even the implied warranty of
# MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
# GNU General Public License at <http://www.gnu.org/licenses/> for
# more details.

FLAGS="--loose-anamorphic -m"
EXT=mkv

while [ $# -gt 0 -a `expr substr "${1:-empty}" 1 2` = "--" ] ; do
    case "$1" in
	--gain) 
	    FLAGS="${FLAGS} $1 $2"
	    shift 2
	    ;;
	--dvd)
	    DVD="$2"
	    shift 2
	    ;;
	--title)
	    TITLE="$2"
	    shift 2
	    ;;
	--chapters)
	    CHAPTERS="$2"
	    shift 2
	    ;;
	--quality)
	    FLAGS="${FLAGS} --quality $2"
	    shift 2
	    ;;
	--crop)
	    FLAGS="${FLAGS} --crop $2"
	    shift 2
	    ;;
	--extension)
	    EXT="$2"
	    shift 2
	    ;;
	--cfr)
	    FR=--cfr
	    shift
	    ;;
	--animation)
	    FLAGS="${FLAGS} -r 23.976 --x264-tune animation --decomb --detelecine"
	    if [ -n "${FR}" ] ; then
		FR=--vfr
	    fi
	    shift
	    ;;
	--pal-animation)
	    FLAGS="${FLAGS} --x264-tune animation --decomb"
	    if [ -n "${FR}" ] ; then
		FR=--vfr
	    fi
	    shift
	    ;;
	--film)
	    FLAGS="${FLAGS} -r 23.976 --x264-tune film --decomb --detelecine"
	    if [ -n "${FR}" ] ; then
		FR=--vfr
	    fi
	    shift
	    ;;
	--decomb)
	    FLAGS="${FLAGS} --decomb"
	    shift
	    ;;
	--subtitles)
	    FLAGS="${FLAGS} --subtitle scan --subtitle-forced"
	    shift
	    ;;
	--lang)
	    FLAGS="${FLAGS} --native-language $2 --native-dub"
	    shift 2
	    ;;
	*)
	    echo unknown flag "$1"
	    exit 1
	    ;;
    esac
done

if [ -n "${TITLE}" ] ; then
    FLAGS="${FLAGS} --title ${TITLE}"
    SOURCE="${DVD:-/dev/dvd}"
    DEST="$1"
else
    SOURCE="$1"
    DEST="$2"
fi

if [ -n "${CHAPTERS}" ] ; then
    FLAGS="${FLAGS} --chapters ${CHAPTERS}"
fi

FLAGS="${FLAGS} ${FR}"

if [ -z "${DEST}" ] ; then
    DEST="${SOURCE%.*}"
    while [ -e "${DEST}${COUNTER}.${EXT}" ] ; do
	if [ -z "${COUNTER}" ] ; then
	    COUNTER=1
	fi
	COUNTER=`expr ${COUNTER} + 1`
    done
    DEST="${DEST}${COUNTER}.${EXT}"
fi

HandBrakeCLI --encoder x264 ${FLAGS} -i "${SOURCE}" -o "${DEST}" 2>/dev/null
